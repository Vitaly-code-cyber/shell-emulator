# Этап 3: проверка загруженной VFS и всех команд, реализованных на
# этом и прошлых этапах. Последней идёт ошибочная команда: скрипт
# останавливается на ней, что подтверждает требование остановки при
# первой ошибке. Скрипт рассчитан на образ deep.
ls
ls -l /home/user
cd /home/user/docs
ls
cd
echo Проверка VFS завершена
history
ls /home/nobody
exit
