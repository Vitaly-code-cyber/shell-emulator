# Этап 3: проверка всех команд, реализованных на этом и прошлых
# этапах. Последней идёт ошибочная команда: скрипт останавливается
# на ней, что подтверждает требование остановки при первой ошибке.
ls
ls -l /home/user
cd
cd /home/user/docs
wget http://example.com
exit
