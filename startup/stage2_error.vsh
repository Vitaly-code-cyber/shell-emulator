# Стартовый скрипт эмулятора: остановка при первой ошибке (этап 2).
ls /var
# Следующая команда неизвестна эмулятору: выполнение прервётся здесь.
wget http://example.com
# Эта строка выполнена не будет.
cd /never/reached
exit
