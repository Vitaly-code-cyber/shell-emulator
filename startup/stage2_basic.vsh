# Стартовый скрипт эмулятора: успешное выполнение (этап 2).
# Строки, начинающиеся с #, являются комментариями и пропускаются.
# Используются команды, не требующие загруженной VFS, поэтому
# скрипт работает и без параметра --vfs.
echo Проверка стартового скрипта
echo
history
history 2
exit
